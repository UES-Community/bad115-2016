---
title: Definición de Índices en SQL (Sintaxis, Variantes y Semántica DDL)
slug: ddl-indices-sql
unit: indexacion-sql
summary: El Lenguaje de Definición de Datos (DDL) funge como el mecanismo sintáctico y el puente comunicacional que el arquitecto invoca para forzar…
order: 5
references:
  - silberschatz-2019
  - elmasri-2016
  - sqlite-docs
  - unidad-4-ref-05
sandbox:
  enabled: true
  dataset: biblioteca
---

El Lenguaje de Definición de Datos (DDL) es el mecanismo sintáctico que los administradores y arquitectos de bases de datos emplean para crear y configurar índices.

A diferencia del estándar DML (como `SELECT` o `JOIN`), que está estrictamente regulado por la norma ANSI SQL, la definición sintáctica y física de los índices varía significativamente entre los distintos motores de bases de datos. Aunque casi todos usan el comando primario `CREATE INDEX`, añaden múltiples extensiones y opciones operacionales específicas.

---

### Sintaxis General de Creación de Índices

El esqueleto sintáctico común responde al siguiente paradigma:

```sql
CREATE [UNIQUE] INDEX nombre_indice  
ON nombre_tabla (columna [ASC|DESC], ...)  
[INCLUDE (columna_adicional, ...)]  
[WHERE condicion_parcial];
```

---

### Desglose de Cláusulas y Variaciones

#### 1. Identificación y Dirección (`ASC` / `DESC`)
Designa el nombre único del índice en la base de datos.
* **Dirección de Ordenación:** Especificar `ASC` (ascendente) o `DESC` (descendente) indica cómo estructurar los punteros.
* Si se omite, el motor asume `ASC` por defecto.

#### 2. Restricción de Unicidad (`UNIQUE`)
Asegura que no se puedan insertar valores duplicados en las columnas indexadas.
* Si el motor detecta un intento de insertar un valor ya existente, abortará la transacción DML lanzando un error de violación de restricción.
* **Creación Automática:** Definir una restricción `PRIMARY KEY` o `UNIQUE` en una tabla genera automáticamente un índice único subyacente para garantizar la restricción con la máxima eficiencia.

#### 3. Índices Parciales o Filtrados (`WHERE`)
Disponible en motores como PostgreSQL y SQL Server. Permite indexar únicamente las filas que cumplen con una condición lógica específica.

```sql
-- Ejemplo de índice parcial para registros activos
CREATE INDEX idx_usuarios_activos 
ON usuarios (email) 
WHERE deleted_at IS NULL;
```

* **Ventaja:** En tablas gigantescas donde el 95% de los datos son históricos o están inactivos, un índice parcial reduce drásticamente el espacio de almacenamiento y acelera las consultas para datos activos.

#### 4. Creación Concurrente (`CONCURRENTLY`)
En entornos de producción críticos que operan "24/7", crear un índice de forma tradicional bloquea las escrituras (bloqueos de escritura o *write-locks*) en la tabla hasta que el proceso finaliza. 

* **El parámetro `CONCURRENTLY` (en PostgreSQL):** Permite crear el índice escaneando la tabla en tramos y sin adquirir bloqueos de escritura.
* Las aplicaciones pueden seguir insertando y actualizando registros de forma normal mientras se genera el índice en segundo plano.

#### 5. Índices de Prefijo (MySQL)
Permite indexar solo una sección inicial (prefijo) de columnas con texto extenso (`VARCHAR`, `TEXT` o `BLOB`).

```sql
-- Indexa solo los primeros 10 caracteres de la columna 'comentario'
CREATE INDEX idx_comentario_prefijo 
ON publicaciones (comentario(10));
```

* **Ventaja:** Minimiza sustancialmente el tamaño físico del índice y optimiza el uso del disco en bases de datos con campos de texto largos.

---

## Práctica guiada

<Callout title="CREATE INDEX">
Un índice acelera las búsquedas por las columnas filtradas. En SQLite, puedes inspeccionar cómo se ejecutará tu consulta utilizando el comando `EXPLAIN QUERY PLAN`.
</Callout>

<ExerciseBlock title="Plan de consulta">
Ejecuta `EXPLAIN QUERY PLAN` sobre una consulta filtrada por la columna `anio` y compara el resultado con un `SELECT *` que no posea ningún filtro.
</ExerciseBlock>

<SQLSandbox dataset="biblioteca" />
